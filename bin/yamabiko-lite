#!/bin/sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
ENTRY="$ROOT_DIR/dist/main.js"

if [ ! -f "$ENTRY" ]; then
  ENTRY="$ROOT_DIR/src/cli/main.ts"
fi

exec sh "$ROOT_DIR/scripts/bun-exec.sh" "$ENTRY" "$@"
